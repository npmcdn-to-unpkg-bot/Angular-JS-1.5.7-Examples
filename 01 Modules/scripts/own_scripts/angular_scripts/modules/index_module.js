/*
 * We set a variable to an angular module.
 * The first parameter that we pass to the module() funtion is the name of the module, in this case indexModule.
 * The second parameter is an array that can have some configurations, for now we are not going to use them, so we let it empty.
 */
var indexModule = angular.module('indexModule', []);